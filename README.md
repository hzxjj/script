# script
```
echo "# script" >> README.md
git init
git add README.md
git commit -m "first commit"
git remote add origin git@github.com:hzxjj/script.git # 第一次使用github，添加个人信息
git push -u origin master   #提交到个人github仓库

```
